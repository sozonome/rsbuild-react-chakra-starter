{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "type-check": {
      "outputs": ["*.tsbuildinfo"]
    },
    "build": {
      "outputs": ["dist/**", "public/**"],
      "env": []
    },
    "lint": {
      "outputs": [],
      "inputs": ["biome.json", "src/**"]
    }
  }
}
